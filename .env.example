# Copy to `.env.local` (or `.env.development.local`) for local overrides.
# Variables prefixed with `VITE_` are exposed to the client.

# API base URL (optional)
# VITE_API_BASE_URL=

# Leverage slider max percent (optional; fallback differs by build mode)
# VITE_MAX_LEVERAGE_PERCENT=150

